import { createStyles } from 'antd-style'

export default createStyles(({ css }) => {
  return {
    skLogoTitle: css`
      padding-left: 8px;
      font-size: 16px;
      font-weight: 700;
    `,
  }
})